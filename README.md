# Amazon Price Drop Monitor

Placeholder README for the TypeScript take-home project.

This project will monitor a configurable set of Amazon-like product pages using fixture-backed HTML by default, persist price checks, detect price drops, notify reviewers, and expose price history.

Live Amazon scraping is intentionally out of scope for the default implementation.

## Setup

Dependencies and runtime instructions will be filled in as implementation steps are completed.
